Usage: "python fileViewer.py filename.txt 30", where 30 is the number of lines to display at a time.

Commands:
	u to move up one page
	d to move down one page.
	t to move to the top (first) page.
	b to move to the bottom (last) page.
	# to move to a given page number.
	q to quit.